# SeqMaker
Portable macOS tool that converts videos to JPEG sequences at user-specified frame rates (just add folder labeled with [#]fps). Auto-organizes output by video name with frame-count labels (video/3fps_786/). Embeds customizable copyright metadata, moves processed files to done/ folder. Runs anywhere, Desktop or external drives. Double-click start/stop.
